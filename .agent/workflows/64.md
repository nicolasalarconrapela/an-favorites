---
description: Fase 2 · develop -> release/x.y.x · merge omitido
---

# Fase 2 · Merge omitido temporalmente

Este fichero existe únicamente para preservar la numeración del workflow.

Reglas:

- no ejecutar merge
- no publicar release
- no usar `MERGE_TITLE_FILE` ni `MERGE_FILE` para ejecutar acciones
- los archivos `MERGE_TITLE_REFRESH.txt` y `MERGE_REFRESH.txt`, si existen, solo quedan preparados para una futura fase de merge

Salida requerida:

- indicar explícitamente que, para `develop -> release/x.y.x`, en esta versión del flujo solo se permite la apertura del Pull Request
- indicar que cualquier merge queda fuera de alcance temporalmente
