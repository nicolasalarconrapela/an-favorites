---
description: Git - Textos de Merge · Caso D (main → develop)
---

# Textos de Merge

Este paso continúa el flujo anterior.
Usar el contexto generado en el workflow previo.
Continuar con las variables generadas en el paso anterior.

---

## Merge

Está permitido leer `"$VAR_S1_FILE"` únicamente para obtener variables necesarias de este paso.

No está permitido usar ninguna otra fuente.

Objetivo:

Generar `MERGE_TITLE.txt` y `merge.txt` dentro de `"$OUTPUT_DIR"` para sincronizar `main` hacia `develop`.

- `MERGE_TITLE.txt` debe contener únicamente la frase principal del merge
- `merge.txt` debe contener únicamente el cuerpo del merge

### 6.3 Generar borrador de MERGE_TITLE.txt y merge.txt

#### MERGE_TITLE.txt

**El título debe ser exactamente:**

```text
Back-merge main into develop after release <RELEASE_VERSION>
```

#### merge.txt

```markdown
Maintenance sync to align `develop` with the final state of `main` after publishing version <RELEASE_VERSION>.
```

---

## Paso anterior

- [6. Generación de textos de PR](file:///c:/Users/Developer/Desktop/@anappwilos/afav_TEST/.agent/workflows/Caso%20D/6.md)

## Siguiente paso

- [8. Creación de PR](file:///c:/Users/Developer/Desktop/@anappwilos/afav_TEST/.agent/workflows/Caso%20D/8.md)
