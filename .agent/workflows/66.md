---
description: Fase 3 · release/x.y.x -> main · merge omitido
---

# Fase 3 · Merge omitido temporalmente

Este fichero existe únicamente para preservar la numeración del workflow.

Reglas:

- no ejecutar merge
- no crear tag
- no publicar GitHub Release
- no subir VSIX

Salida requerida:

- indicar explícitamente que, para `release/x.y.x -> main`, en esta versión del flujo solo se permite la apertura del Pull Request
- indicar que el merge, el tag y la release quedan fuera de alcance temporalmente
