---
description: Fase 4 · main -> develop · merge omitido
---

# Fase 4 · Merge omitido temporalmente

Este fichero existe únicamente para preservar la numeración del workflow.

Reglas:

- no ejecutar merge
- no generar mensaje de back-merge
- no proponer comandos de merge

Salida requerida:

- indicar explícitamente que, para `main -> develop`, en esta versión del flujo solo se permite la apertura del Pull Request
- indicar que el back-merge queda fuera de alcance temporalmente
